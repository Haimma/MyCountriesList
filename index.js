/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './src/App';
import {name as MyCountriesList} from './app.json';

AppRegistry.registerComponent(MyCountriesList, () => App);
